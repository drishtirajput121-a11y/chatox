[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && daphne -b 0.0.0.0 -p $PORT chatox.asgi:application"
healthcheckPath = "/health/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"