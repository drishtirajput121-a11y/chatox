[build]
builder = "nixpacks"

[deploy]
startCommand = "celery -A chatox worker --loglevel=info & celery -A chatox beat --loglevel=info & daphne -b 0.0.0.0 -p $PORT chatox.asgi:application"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
